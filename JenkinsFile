pipeline {
    agent any
    stages {
        stage('Pipeline stage'){
            steps{
                sh "ls"
                sh "pwd"
                sh "chmod +x jenkins/buildScript.sh"
                sh "jenkins/buildScript.sh"
            }
        }
    }
}
