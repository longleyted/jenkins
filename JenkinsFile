pipeline {
    agent any
    stages {
        stage('Pipeline stage'){
            steps{
                sh "pwd"
                sh "ls"
                sh "chmod +x firstpipeline/buildScript.sh"
                sh "firstpipeline/buildScript.sh"
            }
        }
    }
}
