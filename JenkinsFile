pipeline {
    agent any
    stages {
        stage('Pipeline stage') {
            steps{
                sh "pwd"
                sh "ls"
                sh "cd file"
                sh "chmod +x buildScript.sh"
                sh "./buildScript.sh"
            }
        }
    }
}
