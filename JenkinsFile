pipeline {
    agent any
    stages {
        stage('Pipeline stage') {
            steps{
                sh "pwd"
                sh "ls"
                sh "chmod +x /file/buildScript.sh"
                sh "cd file"
                sh "./buildScript"
            }
        }
    }
}
