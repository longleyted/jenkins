pipeline {
    agent any
    stages {
        stage('Pipeline stage'){
            steps{
                sh "ls"
                sh "chmod +x buildScript.sh"
                sh "buildScript.sh"
            }
        }
    }
}
