pipeline {
    agent any
    stages {
        stage('Pipeline stage'){
            steps{
                sh "ls"
            }
        }
stage('Build stage') {
            steps {
                sh "chmod +x jenkins/buildScript.sh"
                sh "buildScript.sh"
            }
        }
    }
}
